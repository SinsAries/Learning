#include <bits/stdc++.h>

using namespace std;

int t, M, X, C[51], H[51];

int main() {
    cin >> t;
    while(t--) {
        cin >> M >> X;
        for (int i = 0; i < M; i++)
            cin >> C[i] >> H[i];
        
    }

    return 0;
}